{
  "name": "boxmp",
  "version": "1.0.0",
  "scripts": {
    "prestart": "coffee --bare --compile public/scripts/",
    "start": "coffee server.coffee"
  },
  "dependencies": {
    "coffee-script": "^1.12.4",
    "express": "^4.14.1",
    "socket.io": "^1.7.3"
  }
}
